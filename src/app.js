const express = require('express'); const app = express(); const bodyParser = require('body-parser'); const bookingsRouter = require('./routes/bookings'); app.use(bodyParser.json()); app.use('/api/bookings', bookingsRouter); app.listen(3000, () => { console.log('Server is running on port 3000'); });